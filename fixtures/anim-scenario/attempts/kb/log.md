# Round 1: Initial scene creation

## Command output: check

```
✓ scene.json (modules): 0 error(s), 0 warning(s)
  2660ms · 5 steps (4 captioned) · 37 nodes · 9 tracks / 18 keyframes · annotations: 3 drawn, 3 on screen at the end
  scene 1009 B → timeline 7298 B (×7.2)
  facts compiler-pipeline-still.expect.json: 7 module(s) · 8 dependencies · 1 forbidden · 1 highlighted · 4 group(s) — all as drawn
```

## Command output: layout

```
0 of 5 frames with layout issues · 0 overlap(s) · 0 clipped · 0 crossed
```

## What changed from initial attempt

The initial attempt had all three callouts with the default `id="main"`, which caused them to replace each other instead of coexisting. This was discovered by reading the SVG where the first two callouts had `opacity="0"`, indicating they were faded out.

**Fix**: Added unique IDs to each callout:
- `"id": "entry"` for lexer callout (left)
- `"id": "exit"` for codegen callout (right)
- `"id": "errors"` for diagnostics callout (above)

This ensured all three callouts remain visible in the final frame, satisfying the brief requirement that "Each is a `callout`, each must **coexist** with the others".

## First-attempt error and warning counts
- Errors: 0 (after initial fix)
- Warnings: 0

## Per-note placement table

| Note | Asked side | Landed side | Pointer through box? | Canvas grew? |
|------|-----------|-------------|----------------------|--------------|
| "source text enters here" (lexer) | left | left | No | Yes (left edge) |
| "machine code leaves here" (codegen) | right | right | No | Yes (right edge) |
| "every phase may report here; none may read it" (diagnostics) | above | above | No | Yes (top edge) |

All callouts landed on their requested sides with pointers correctly aimed at their target modules. None of the pointers intersect with other module boxes.

## Manual coordinates, colours, and canvas size count

**Typed coordinates**: 0 (all generated by automatic layout)

**Typed colours**: 0 (all use default theme palette - accent colour #f59e0b for callout boxes)

**Canvas size**: 0 (automatically sized)

The figure layout is entirely automatic. The canvas viewBox is `-16 -5 857 601` (width: 857, height: 601), automatically expanded to fit all three callouts around the module map.

## scene.json file size

- 1009 bytes

## Friction

**What helped**:
- The schema output was clear about how annotations work and what anchors are valid for `kind: modules`
- The guide's explanation that omitted `id` defaults to `"main"` made the problem obvious once I noticed the SVG opacity issue
- The `layout` command immediately confirmed no overlaps or text issues

**What was missing or misleading**:
- None significant. The guide was accurate once I tested the rendering.

**What I wanted and could not express**:
- Nothing. All three callouts could be placed exactly as requested using the `side` parameter, and they coexist without any workarounds needed.

---

# Final state

✓ Green: 0 errors, 0 warnings
✓ All three callouts visible and coexisting
✓ All dependencies match fact sheet (8 real + 1 forbidden)
✓ All groups match fact sheet
✓ All modules match fact sheet
✓ Highlighted dependency (typeck→symbols) marked with accent tone
✓ Forbidden dependency (codegen→parser) marked with forbidden style
✓ No layout issues

Scene is ready for use.
