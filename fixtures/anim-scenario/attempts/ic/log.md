# 決済のタイムアウトと再試行（日本語分散システム図）制作ログ

## ラウンドごとの進捗

### Round 1: 初期シーンの構築と日本語ラベル対応

**初回 check 結果**: ✗ 0 個, ⚠ 0 個

**作業内容**:
1. `kind: distributed` のシーンテンプレートを作成
   - ノード: 注文サービス、決済ゲートウェイ、カード会社（全て日本語ラベル）
   - 6 つのメッセージを定義：課金要求①、オーソリ、承認、課金完了（失敗）、課金要求②、課金完了応答
   - `lost: true` で課金完了の応答が失われることを表現
   - `after` と `delay` で再試行のタイミングを制御

2. 冪等キーの同一性を視覚化するため `relate` 注釈を追加
   - 初版: `relate` ラベル「冪等キー: A1（同一）」は 40px 越境
   - 修正: 「キー A1（同一）」に短縮→9px 越境
   - 最終版: 「A1（同一）」に短縮→✓ 0 警告達成

3. メッセージラベルを英字から日本語に変更
   - `charge_1` → `課金リクエスト1`
   - `auth` → `オーソリ`
   - `approval` → `承認`
   - `complete_lost` → `課金完了`
   - `charge_2` → `課金リクエスト2`
   - `complete_existing` → `課金完了応答`

4. メッセージの「after」ターゲットと「relate」のアンカーを日本語に更新

## 最終確認結果

- ✓ check: 0 errors, 0 warnings
- ✓ layout: 0 frames with issues
- ✓ explain: 7 steps, 7 captions (順序が正しい)
- ✓ video: GIF 成功（107 frames, 8550ms）

## 日本語ラベルに関する知見

**ガイドの対応状況**:
- 「ノード・メッセージラベルは任意スクリプト可」と書かれており、実装は問題なかった
- CJK 文字の幅計算が正確で、日本語テキストは自動で折り返しや配置がされている
- 注釈（`relate` ラベル）も同様に処理され、オーバーフローは警告で教える仕組み

**ラベルの幅に関する工夫**:
- 「冪等キー: A1（同一）」→「キー A1（同一）」→「A1（同一）」と段階的に短縮
- 各メッセージラベルは 4～5 文字に収まり、レイアウトに問題なし

**何が困ったか**:
- 特になし。ガイドの「Captions may wrap … between glyphs where there are none」という説明が正確だった

## 手で書いた座標・色・キャンバスサイズ

**なし**: すべてコンパイラが自動配置

## 図に入れたかったが表現できなかったこと

**なし**: 必要な情報（タイムアウト、同じキー、二重課金の防止メカニズム）はすべて表現できた

## 最終ファイル構成

- `scene.json`: 1252 B（ソース）→ 8165 B（タイムライン）
- `payment.gif`: 1264 KB, 640×400, 107 フレーム

すべての要件を満たし、説明順序は正確です。
