{
  "modules": ["anim", "animation-eval", "markup", "vlmkit (cli)", "mcp", "generate", "ai", "core", "plan", "capture", "heal"],
  "deps": [
    "anim->animation-eval",
    "markup->anim",
    "markup->animation-eval",
    "markup->core",
    "markup->ai",
    "animation-eval->core",
    "vlmkit (cli)->mcp",
    "vlmkit (cli)->markup",
    "vlmkit (cli)->anim",
    "vlmkit (cli)->ai",
    "vlmkit (cli)->core",
    "vlmkit (cli)->plan",
    "vlmkit (cli)->heal",
    "mcp->ai",
    "plan->ai",
    "plan->core",
    "capture->ai",
    "capture->core",
    "heal->capture",
    "heal->ai",
    "heal->core"
  ],
  "forbidden": [],
  "groups": {},
  "nesting": {},
  "highlighted": [],
  "issues": [
    { "kind": "crossed", "what": "near the 'ai' and 'core' boxes many arrow tails from vlmkit (cli), mcp, markup, plan, capture and heal converge and overlap, so individual lines are hard to trace back to one exact source with certainty" },
    { "kind": "other", "what": "the 'generate' box (between mcp and ai) shows no clearly visible arrow touching it, unlike every other counted module" },
    { "kind": "crossed", "what": "the long diagonal lines running from vlmkit (cli) up to markup and anim pass close beside/over the mcp and generate boxes, making it ambiguous whether they start at cli or at one of those boxes" }
  ],
  "notes": "All arrows appear solid black (no red-dashed or grey-dashed lines seen), so 'forbidden' is empty and no dashed-style dependency is called out. No rounded container groups distinct from the module boxes themselves were visible, so groups/nesting are empty. Direction and exact source of several arrows feeding into ai/core (especially from markup, plan, capture, heal) were read as best-guess given heavy line crossing near that cluster; the arrow into markup from below (possibly involving generate or ai) is the least certain edge in this reading."
}
