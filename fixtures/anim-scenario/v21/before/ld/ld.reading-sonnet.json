{
  "modules": ["index", "route-cap", "capturer", "cap-config", "pw-analyzer", "pw-launch-err", "viewport", "cf-actions", "gates", "batch-pre", "prescanner", "cr-smoke", "cr-client", "cr-wasm", "det-types"],
  "deps": [
    "index->capturer",
    "index->cap-config",
    "index->pw-analyzer",
    "index->pw-launch-err",
    "index->viewport",
    "index->cf-actions",
    "index->cr-client",
    "index->cr-wasm",
    "index->det-types",
    "route-cap->cr-client",
    "viewport->cf-actions",
    "gates->cr-smoke",
    "batch-pre->prescanner",
    "prescanner->cr-smoke",
    "batch-pre->cr-client",
    "cr-smoke->cr-client",
    "cr-smoke->det-types",
    "cr-wasm->det-types"
  ],
  "groups": {
    "public surface": ["index"],
    "Playwright side": ["route-cap", "capturer", "cap-config", "pw-analyzer", "pw-launch-err", "viewport", "cf-actions"],
    "Crater side": ["gates", "batch-pre", "prescanner", "cr-smoke", "cr-client", "cr-wasm"],
    "shared types": ["det-types"]
  },
  "nesting": {},
  "highlighted": [],
  "issues": [
    { "kind": "crossed", "what": "the long fan-out arrows from 'index' run almost horizontally all the way to 'cr-client', 'cr-wasm' and 'det-types', passing very close under the 'batch-pre', 'prescanner' and 'cr-smoke' boxes and the 'Crater side' container label on the way — hard to tell whether they graze or actually cut through those boxes" },
    { "kind": "crossed", "what": "the single line out of 'route-cap' crosses through the middle of the index fan-out bundle around the 'cap-config' / 'pw-analyzer' region, making it hard to isolate as a distinct edge" },
    { "kind": "other", "what": "in the dense bundle of near-parallel lines converging on the 'cr-client' / 'cr-wasm' / 'det-types' cluster, individual line endpoints are difficult to trace back to a single unambiguous source, so the exact count and pairing of arrows into that cluster is uncertain" }
  ],
  "notes": "The fan of ~9 lines out of 'index' is the dominant and hardest-to-read feature: all lines leave the same corner of the box at slightly different angles, so tracing which line ends at which far-right box (cr-client vs cr-wasm vs det-types) required following slope by eye rather than a clean visual path. Arrowheads at cr-smoke/cr-client/det-types are also small and clustered, so the cr-smoke<->cr-client and cr-smoke->det-types directions are my best reading rather than certain."
}
